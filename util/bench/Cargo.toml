[package]
name = "sekien-bench"
version = "0.1.0"
edition = "2021"
description = "Benchmark harness for sekien — measures wall time and max RSS, compares against mmdc if available"
license = "MIT OR Apache-2.0"
publish = false

[[bin]]
name = "bench"
path = "src/main.rs"

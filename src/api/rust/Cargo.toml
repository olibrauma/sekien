[package]
name = "sekien-api"
version = "0.1.0"
edition = "2021"
description = "sekien-api — Rust wrapper for invoking the sekien binary"
license = "MIT OR Apache-2.0"

[lib]
path = "src/lib.rs"

[dependencies]
thiserror = "2"
